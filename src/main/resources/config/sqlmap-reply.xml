<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Maper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="ReplyDAO">
	<insert parameterType="hashmap" id="create">${sql} </insert>

<select parameterType="hashmap" id="readAll" resultType="reply">${sql} </select>

<delete parameterType="hashmap" id="delete">${sql} </delete>

<update parameterType="hashmap" id="update">${sql} </update>
</mapper>
